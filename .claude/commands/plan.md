# /plan — Feature Implementation Plan

Produce an implementation plan for the described feature WITHOUT writing code.

Steps:
1. Identify which module(s) and which `.claude/skills/*` apply; read them.
2. Restate the relevant non-negotiables (determinism, engine authority, scale discipline, provider neutrality) for this feature.
3. List spec changes needed in `docs/specs/` first.
4. Break the work into ordered, testable steps; name the subagent best suited to each.
5. Call out risks (determinism, scale, fog-of-war/security) explicitly.

Output a concise plan; do not implement.
