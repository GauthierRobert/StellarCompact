# backend/ — Spring Boot 4 / Java 25 (not yet implemented)

Empty by design (Phase 1+). Modules to create: engine (pure), galaxy (lib),
agent-runtime (Spring AI), orchestrator, api, persistence, app.
Read `.claude/skills/game-engine-determinism`, `agent-sovereign`, `spring-ai-agent`
and `docs/architecture/` before coding. Engine stays framework-free and deterministic.
