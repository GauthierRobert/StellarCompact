# frontend/ — Angular 21 app (not yet implemented)

Empty by design (Phase 2+). Before adding code, read `.claude/skills/angular21-signals`,
`galaxy-rendering`, `lod-tiling`, `realtime-websocket`, and the specs in `docs/specs/`.
Port the camera model from `poc/galaxy-navigator.html`.
