import { Component } from '@angular/core';

/**
 * Sovereign config screen — placeholder shell.
 * Will host the faction / persona configuration UI (E7-05).
 */
@Component({
  selector: 'app-config',
  template: `<p>config</p>`,
  styles: [],
})
export class ConfigComponent {}
