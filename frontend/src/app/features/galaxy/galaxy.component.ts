import { Component } from '@angular/core';

/**
 * Galaxy view — placeholder shell.
 * Will host the WebGL2 galaxy renderer (E7-02 / E8-01).
 * Coordinate with galaxy-renderer-engineer on the WebGL component boundary.
 */
@Component({
  selector: 'app-galaxy',
  template: `<p>galaxy</p>`,
  styles: [],
})
export class GalaxyComponent {}
