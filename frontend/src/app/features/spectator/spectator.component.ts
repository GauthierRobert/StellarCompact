import { Component } from '@angular/core';

/**
 * Spectator view — placeholder shell.
 * Will host the public event feed, leaderboard, and victory-progress (E7-06).
 */
@Component({
  selector: 'app-spectator',
  template: `<p>spectator</p>`,
  styles: [],
})
export class SpectatorComponent {}
